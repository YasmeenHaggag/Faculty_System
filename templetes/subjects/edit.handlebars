<h1>Edit {{subject.name}}</h1>


<form action='/subjects/{{subject._id}}?_method=PUT' method='POST'>
    {{!-- Name --}}
    <div>
  <label for="">Name</label>  

    <input type="text" class="form-control" name="name" value="{{subject.name}}"/>  
    </div>


       {{!-- Code --}}
    <div>
  <label for="">Code</label>  

    <input type="text" class="form-control" name="code" value="{{subject.code}}"/>  
    </div>

    {{!-- Required_Subjects --}}
    <div>

      <label for="">Required Subject</label>  

    <input type="text" class="form-control" name="required_subject" value="{{subject.required_subject}}"/>  
      <!--<label for="country">Required Subject</label>
       <select name="required_subject" id="required_subject" class="form-select">
    
          {{#each subject}}

          <option value="{{_id}}">{{name}}</option>

          {{/each}}
       </select> -->

     
    </div>

       {{!-- Department --}}
    <div>
  <label for="">Department</label>  

     <select name="department" id= "" class="form-select">
    {{#each department}}

<option value="{{_id}}">{{name}}</option>

    {{/each}} 

     </select> 
    </div>
    <div class="mt-3">
        <button class="btn btn-success" type="submit">
       Save
        </button>
    </div>
</form>